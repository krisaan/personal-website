---
layout: base.njk
title: "All Events"
---

<section class="space-y-4">
    <h1 class="text-2xl font-bold text-events lowercase">all events</h1>
    <div class="list-bracket text-events/40 dark:text-events/30">
        <ul class="space-y-3 text-sm">
            {% for item in collections.events %}
                <li class="leading-relaxed">
                    <a href="{{ item.url }}" class="text-lightLink dark:text-darkLink underline hover:text-opacity-80 transition-colors font-semibold">
                        {{ item.data.title }}
                    </a> 
                    {% if item.data.date %}
                        <span class="text-stone-500 italic">({{ item.data.date }})</span>
                    {% endif %}
                    {% if item.data.category %}
                        <span class="text-stone-400">·</span> 
                        <span class="text-stone-600 dark:text-gray-400 italic">{{ item.data.category }}</span>
                    {% endif %}
                    {% if item.data.summary %}
                        <span class="text-stone-400">·</span> 
                        <span class="text-stone-600 dark:text-gray-400">{{ item.data.summary }}</span>
                    {% endif %}
                </li>
            {% endfor %}
        </ul>
    </div>
</section>